---
/**
 * JoinMovementCTA — Tri-choice CTA section (Instance 7).
 *
 * Presents 3 participation paths + a unified full form with checkboxes,
 * wrapped in a CloudsCTA with center layout.
 *
 * Used on: Homepage bottom (Placement 2), Contact page (Placement 9).
 *
 * Props:
 *   headingTag – 'h1' | 'h2' (default 'h2')
 */
import CloudsCTA from './CloudsCTA.astro';
import FormWaitlist from './FormWaitlist.astro';

interface Props {
  headingTag?: 'h1' | 'h2';
  formId?: string;
}

const { headingTag = 'h2', formId } = Astro.props;
---

<CloudsCTA
  heading="Join the Movement"
  subheading="There are three powerful ways you can participate."
  headingTag={headingTag}
  layout="center"
  maxWidth="max-w-5xl"
  sectionClass="flex-1"
>
  <!-- 3 Option Cards -->
  <div class="grid grid-cols-1 md:grid-cols-3 gap-6 mb-12">
    <!-- 1. Waitlist -->
    <div class="bg-fam-purple rounded-xl p-6 text-center text-white">
      <div class="text-3xl mb-3">👨‍👩‍👧‍👦</div>
      <h3 class="text-xl font-bold mb-2">Join the FamCentral Waitlist</h3>
      <p class="text-white/80 text-sm leading-relaxed">Become part of a growing community of families choosing connection.</p>
      <p class="text-white/80 text-sm leading-relaxed">Get early access to tools, rewards, and exclusive launch perks.</p>
    </div>
    <!-- 2. Founding Partner -->
    <div class="bg-fam-purple rounded-xl p-6 text-center text-white flex flex-col items-center">
      <div class="text-3xl mb-3">📈</div>
      <h3 class="text-xl font-bold mb-2">Become a Founding Partner</h3>
      <p class="text-white/80 text-sm leading-relaxed">Help us bring FamCentral to your city.</p>
      <p class="text-white/80 text-sm leading-relaxed">Build the ecosystem and earn from the ground up.</p>
    </div>
    <!-- 3. Expert Faculty -->
    <div class="bg-fam-purple rounded-xl p-6 text-center text-white">
      <div class="text-3xl mb-3">🎓</div>
      <h3 class="text-xl font-bold mb-2">Apply to Join Our Expert Faculty</h3>
      <p class="text-white/80 text-sm leading-relaxed">Share your message and impact families nationwide.</p>
      <p class="text-white/80 text-sm leading-relaxed">Join a curated network of trusted voices and leaders.</p>
    </div>
  </div>

  <!-- Form in white card (via form slot) -->
  <div slot="form" class="max-w-md mx-auto">
    <div class="bg-white rounded-xl p-8 text-fam-text">
      <FormWaitlist
        variant="full"
        buttonText="Join the Waitlist"
        buttonClass="bg-fam-red hover:bg-fam-red-dark"
        formId={formId}
      />
    </div>
  </div>
</CloudsCTA>
